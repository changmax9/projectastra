# TODO

## Active PDF Import Goals

- [in progress] P1: Improve scanned/image-only MCQ recovery without over-generation.
  Embedded-text packet recovery is now strong for the current real probes: `AP Chem 2023.pdf` remains 67 drafts (60 MCQ, 7 FRQ), `qp-2024-chemistry.pdf` now reaches 67 drafts (60 MCQ, 7 FRQ), and `practice exam 2016(1).pdf` now reaches 61 drafts (60 MCQ, 1 FRQ). Scanned OCR now restores dropped punctuation on question-like starts, normalizes only complete sequential A-D/E choice-label runs with source-page warnings, uses automatic layout segmentation by default, and uses a bounded document-wide triage sample instead of blindly OCRing the first pages of large image-only mixed packets. Low-confidence/tiny OCR fragments inside detected visual regions are excluded only from normalized segmentation text while untouched raw OCR remains available for audit. Remaining work is line/geometry recovery for partially merged OCR options, especially math-heavy scans, plus true later-batch extraction of triage-skipped pages. Do not start custom Tesseract training until a labeled page-image/ground-truth evaluation corpus exists.

- [in progress] P1: Add precise visual/table/diagram crop proposals and review controls.
  Visual-reference drafts now get source-page previews for OCR and text PDFs, candidate evidence is not saved automatically, full-page PDF preview URLs are rejected from saved question images, and the review card has explicit cropped-asset URL/caption/bbox controls. PyMuPDF image blocks, vector drawing clusters, table-like drawing grids, bounded non-text regions detected on scanned pages, and worker-only AI visual evidence blocks are preserved without perturbing normalized text reading order. Bounded candidates render into `/uploads/pdf-import-crops/...`, rank against nearby question text with above/below and horizontal proximity cues, and stay review-only until an admin explicitly keeps them. Remaining work is repeatable integration coverage with a real embedded raster-image block, visually complex multi-figure packets, and manual crop correction ergonomics.

- [in progress] P2: Make imports truly page-chunk resumable.
  The production path now queues leased Supabase jobs for a single Railway Docker worker, persists 12-page batch progress, retries failed/cancelled batches, resumes completed batches after interruption, and finalizes stable drafts only after all pages finish. Vercel now fails closed to remote-worker polling even if `PDF_OCR_MODE` is omitted, and the worker has a no-claim `PDF_WORKER_SMOKE=1` startup check. Remaining deployment work is applying the `pdf_uploads` storage-column portion of migration `009`, creating/configuring the two R2 buckets, and provisioning Railway/Vercel environment variables.

- [pending] P2: Add authenticated browser/E2E coverage for `/admin/pdfs`.
  Live Supabase browser verification now covers authenticated upload, import start, progress completion, same-page inline draft display, and review-only persistence for `practice exam 2016(1).pdf`. The latest local pass improved workflow landmarks and verified authenticated `/admin/pdfs` rendering with an HTTP smoke because the Codex browser bridge failed before attaching to the in-app tab (`failed to write kernel assets`). Add a seeded mock admin test for resume, save-as-draft, reject, and no-direct-publish checks without mutating live question data.

- [blocked] P2: Resolve Supabase seed/account setup consistency.
  Local Supabase connectivity is confirmed, with one admin profile and no profile/Auth mismatches in the read-only probe. Read-only exam inspection found live Supabase has `20230000-0000-4000-8000-000000000150` (`AP Physics 1 2023 Practice Exam`) but not the mock seeded exam id/title `20000000-0000-4000-8000-000000000001` (`AP Physics 1 Mock Exam 1`). Completing this requires an explicit Supabase data-change decision because current rules prohibit seeding or changing live data.

## Train Log

- 2026-05-24: Cleared implemented items from this TODO. Historical completed work and command logs live in `docs/DEVELOPMENT_LOG.md`.
- 2026-05-24: Normalized confidence values to `0..1` in the analyzer/review path. Synthetic OCR probe max confidence is now `0.9404`, and garbled OCR reports `0.4429` instead of `44.29`.
- 2026-05-24: Implemented section-aware segmentation/filtering for real PDFs. The filter removes table-of-contents/scoring sections and out-of-sequence FRQ-like starts; `AP Chem 2023.pdf` now lands at 67 drafts, while `qp-2024-chemistry.pdf` lands at 64 drafts and still needs MCQ recovery work.
- 2026-05-24: Added conservative explicit answer-key association. Synthetic answer-key-at-end now attaches cited answers, real `practice exam 2016(1).pdf` matches 55 explicit MCQ answers, and `AP Chem 2023.pdf`/`qp-2024-chemistry.pdf` do not attach answers from scoring/question pages.
- 2026-05-24: Confirmed local Supabase is connected read-only and added admin dashboard account-health reporting. Live probe found 5 profiles, 1 admin profile, 4 student profiles, 5 Auth users, and no profile/Auth mismatches; `scripts/check-supabase-data.cjs` still reports a missing expected seeded exam.
- 2026-05-24: Implemented content-based scoring-guide/rubric/sample-response detection. A neutral-filename rubric probe now fails closed with zero drafts; real question-packet draft counts stayed stable (`AP Chem 2023.pdf` 67, `qp-2024-chemistry.pdf` 64, `ap25-frq-chemistry.pdf` 3 FRQ drafts).
- 2026-05-24: Added fixture-backed analyzer regression tests for explicit answers/explanations, neutral scoring packets, and mixed prompt/scoring packets. Also added explicit explanation-source association and safer visual review controls that require cropped asset URLs instead of saving full-page PDF previews.
- 2026-05-25: Stabilized PyMuPDF raw block handling so image blocks are preserved for crop candidates without affecting text reading order. Added bounded crop rendering for image-block candidates, explicit incomplete-visual warnings when no crop is available, and regression/source checks for the new crop path.
- 2026-05-31: Verified migration `008` is present in GitHub and complete in the configured live Supabase project. Hardened the schema preflight across all four PDF-import tables, replaced start-import runtime overlays with admin warnings, preserved Python worker stderr, added macOS `python3` fallback, and added read-only `npm run check:supabase:pdf-import`.
- 2026-06-01: Exercised the authenticated `/admin/pdfs` website against live Supabase with explicit authorization. Uploaded and analyzed `practice exam 2016(1).pdf` into review-only job `pdf_job_91clwyo1_mpv8vhcj`: 62 text pages, 56 pending drafts, 0 saved questions, and no direct publish.
- 2026-06-02: Added an admin-dashboard PDF-import review queue, direct review links, a pending-draft overview stat, and clearer `PDF Imports` sidebar wording after confirming that live Microeconomics OCR drafts were present but difficult to discover.
- 2026-06-02: Recovered missing embedded-text MCQs conservatively and improved scanned OCR choice recovery. Geometry ordering now pairs overlapping accessibility-description choice blocks with their numbered prompt, lowercase formula starts no longer collide with decimal measurements, broader duplicate MCQ candidates are filtered, and empty trailing page markers no longer spill confidence/page attribution into the prior question. Added validated `PDF_OCR_PSM` overrides and conservative OCR boundary/choice-label normalization with admin-verification warnings.
- 2026-06-02: Added review-only PDF-native table and vector-figure crop proposals. Drawing clusters are classified cheaply, associated with nearby draft anchors using source-page and above/below cues, cropped to bounded PNG evidence, and never auto-kept or auto-published.
- 2026-06-03: Improved `/admin/pdfs` workflow clarity. The page now labels PDF Imports, shows compact upload/analyze/review/save-draft stages, highlights the selected import, surfaces a next-action panel with draft/page-audit jumps, and makes draft save controls say verified draft/draft-only/no-publish.
- 2026-06-11: Added bounded document-wide OCR triage for large image-only mixed packets. Astra now samples pages across the document, scores likely prompt/choice pages while penalizing scoring-guide/reference material, and sends the strongest page neighborhoods to full OCR instead of always selecting the first pages.
- 2026-06-12: Added scanned-page visual-region detection, geometry-aware OCR line audit blocks, diagram-noise filtering for normalized segmentation text, single-visible-subpart FRQ recovery, and a bounded page-scaled OCR timeout. The supplied 718-page Physics packet now completes its 12-page triage OCR pass with 26 review drafts and 50 review-only crop candidates instead of timing out with zero drafts.
- 2026-06-13: Added the production remote-worker path: direct multipart R2 uploads up to 500 MB, leased Supabase jobs and resumable page batches, Railway worker packaging, durable R2 evidence uploads, stable post-extraction finalization, and admin queue/cancel/retry/delete controls.
- 2026-06-17: Hardened the production OCR worker path: Vercel defaults to remote-worker polling when `PDF_OCR_MODE` is omitted, PDF/R2 API routes are explicitly short-lived Node routes, multipart completion validates active uploads and unique part metadata, cancelled batches are marked visibly, retries revive cancelled batches, and the deep-link import page now exposes progress, raw OCR audit output, candidate evidence, cancel/retry, and delete controls.
- 2026-06-18: Added the worker-only hybrid visual evidence stage. `PDF_VISUAL_AI_MODE=off|audit|assist` defaults off; audit mode stores provider markdown/regions as untrusted review-only evidence, assist mode may feed provider bboxes into crop-candidate generation, and Vercel routes remain upload/enqueue/poll only. Added the fixture-backed visual-AI evaluation harness for pages 150, 170, 205, 255, 570, and 610.
- 2026-06-18: Hardened visual-AI production mode with worker startup preflight, remote-worker config checks, per-page provider timeouts, bounded page-image size, transient retry, fail-open default behavior, optional provider image extraction for R2 evidence upload, and a guard against accidentally using the fixture provider on Railway.
- 2026-06-18: Turned the visual-AI eval into a golden-set harness. `tests/fixtures/pdf-visual-ai-golden.json` records current hard-page invariants and future targets, `npm run eval:visual-ai` now reports per-page checks plus recall/bad-crop/draft-overgeneration/cost/latency metrics, and the PDF review card can label candidate evidence as useful, wrong-region, missing-graph, or bad-segmentation for later threshold/prompt tuning.
- 2026-06-19: Persisted candidate visual-evidence reviewer feedback on `pdf_import_draft_assets` metadata without a new migration and added `npm run report:visual-feedback` to summarize useful/wrong/missing/bad-segmentation labels from Supabase.
